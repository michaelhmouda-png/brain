import webPush from 'web-push';import { createSupabaseServer } from '@/lib/supabaseServer';
type Claim={job_id:string;lease_token:string;endpoint:string;p256dh:string;auth_key:string;notification_id:string;title:string;summary:string;route:string};
function first(value:unknown):Record<string,unknown>|null{const item=Array.isArray(value)?value[0]:value;return typeof item==='object'&&item!==null&&!Array.isArray(item)?item as Record<string,unknown>:null;}
function claim(value:unknown):Claim|null{const r=first(value);if(!r||!['job_id','lease_token','endpoint','p256dh','auth_key','notification_id','title','summary','route'].every(k=>typeof r[k]==='string'))return null;return r as unknown as Claim;}
export async function processNotificationWork(){const supabase=createSupabaseServer();await supabase.rpc('generate_task_reminder_obligations');const {data:outbox}=await supabase.rpc('claim_notification_outbox',{p_lease_seconds:120});const obligation=first(outbox);if(obligation&&typeof obligation.outbox_id==='string'&&typeof obligation.lease_token==='string'){const {error}=await supabase.rpc('materialize_notification_outbox',{p_outbox_id:obligation.outbox_id,p_lease_token:obligation.lease_token});if(error)await supabase.rpc('fail_notification_outbox',{p_outbox_id:obligation.outbox_id,p_lease_token:obligation.lease_token,p_code:error.code??'MATERIALIZE_FAILED'});}
 const {data}=await supabase.rpc('claim_notification_delivery',{p_lease_seconds:120});const job=claim(data);if(!job)return obligation?'materialized':'idle';
 const publicKey=process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY,privateKey=process.env.VAPID_PRIVATE_KEY,subject=process.env.VAPID_SUBJECT;if(!publicKey||!privateKey||!subject)throw new Error('VAPID_CONFIGURATION_MISSING');webPush.setVapidDetails(subject,publicKey,privateKey);
 try{await webPush.sendNotification({endpoint:job.endpoint,keys:{p256dh:job.p256dh,auth:job.auth_key}},JSON.stringify({title:job.title,summary:job.summary,notificationId:job.notification_id,route:job.route}),{TTL:300,urgency:'normal'});const {error:completionError}=await supabase.rpc('complete_notification_delivery',{p_job_id:job.job_id,p_lease_token:job.lease_token});if(completionError){console.warn('[Notification Worker] provider accepted but completion persistence failed',{stage:'notification.delivery.complete',jobId:job.job_id,notificationId:job.notification_id,errorCode:completionError.code??'DELIVERY_COMPLETION_FAILED'});throw new Error('DELIVERY_COMPLETION_FAILED');}return'delivered';}catch(error){const status=typeof error==='object'&&error!==null&&'statusCode'in error&&typeof error.statusCode==='number'?error.statusCode:0;const permanent=status===404||status===410;await supabase.rpc('fail_notification_delivery',{p_job_id:job.job_id,p_lease_token:job.lease_token,p_code:permanent?'SUBSCRIPTION_EXPIRED':'PUSH_DELIVERY_FAILED',p_permanent:permanent});return permanent?'revoked':'retry';}}
